interface:
  display_name: "Universal Mobile Deploy"
  short_description: "Analyze the real project first, then choose Expo/EAS, Fastlane, Capacitor, native deployment, and related release services correctly"
  default_prompt: "Use $universal-mobile-deploy-skill to inspect this repo, classify the real mobile stack, reuse any existing deployment setup, install the correct missing native dependencies, verify related release services like RevenueCat and EAS Update when relevant, and execute the right deployment path with Expo/EAS, Fastlane, Capacitor, or native tooling instead of forcing the wrong workflow."
